#ifndef MAIN_H
#define MAIN_H

#include "engine.h"

extern RenderLayer* background_layer;
extern RenderLayer* world_layer;
extern RenderLayer* hud_layer;
extern RenderLayer* mouse_layer;
extern sf::Font* main_font;

#endif//MAIN_H
